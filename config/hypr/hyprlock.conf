general {
    hide_cursor = false
    grace = 0
    ignore_empty_input = true
    immediate_render = true
    text_trim = true
}

background {
    monitor =
    color = rgba(18, 20, 29, 1.0)
}

label {
    monitor =
    text = cmd[update:1000] echo "$(date +"%H:%M")"
    color = rgba(197, 202, 221, 1.0)
    font_size = 90
    font_family = JetBrainsMono Nerd Font
    halign = center
    valign = center
    position = 0, 150
}

label {
    monitor =
    text = cmd[update:60000] echo "$(date +"%A, %d %B")"
    color = rgba(122, 130, 150, 1.0)
    font_size = 20
    font_family = JetBrainsMono Nerd Font
    halign = center
    valign = center
    position = 0, 60
}

input-field {
    monitor =
    size = 300, 50
    outline_thickness = 2
    dots_size = 0.3
    dots_spacing = 0.3
    dots_center = true
    fade_on_empty = false
    font_color = rgba(197, 202, 221, 1.0)
    inner_color = rgba(36, 40, 55, 1.0)
    outer_color = rgba(142, 163, 201, 1.0)
    check_color = rgba(232, 131, 136, 1.0)
    fail_color = rgba(232, 131, 136, 1.0)
    placeholder_text = <span foreground="##7a8296">Enter password...</span>
    hide_input = false
    position = 0, -80
    halign = center
    valign = center
}
